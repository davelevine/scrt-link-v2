<script lang="ts">
	// Billing is disabled on this instance — the server `load` redirects to the
	// organization overview before this renders. Minimal placeholder so the route resolves.
</script>

<p class="text-center">Redirecting…</p>
