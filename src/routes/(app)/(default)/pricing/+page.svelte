<script lang="ts">
	// Billing is disabled on this instance — the server `load` redirects to `/`
	// before this ever renders. Kept as a minimal placeholder so the route resolves.
	import { CenteredPage } from '$lib/components/page';
</script>

<CenteredPage title="Pricing">
	<p class="text-center">Redirecting…</p>
</CenteredPage>
